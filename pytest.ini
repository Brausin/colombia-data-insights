[pytest]
; Limita la colección a los tests del repo: evita recoger suites de clones
; anidados (p. ej. factura-co/) cuyos app/ui.py chocan en sys.path.
testpaths = tests
norecursedirs = factura-co .git data scripts
